# The release ZIP workflow replaces this with the published release tag.
VISITORPORTAL_VERSION=RELEASE_VERSION_PLACEHOLDER

# Advanced override. Usually not needed.
# VISITORPORTAL_IMAGE=ghcr.io/p0etinc0de/besucherportal:v1.0.2

# Optional host port overrides if 8080 or 8025 are already in use.
# APP_PORT=8080
# MAILHOG_PORT=8025
